// import { getDates } from './src/utils/getDates';

// const RRULE = ({
//   day,
//   month,
//   year,
// }: {
//   day: string;
//   month: string;
//   year: number;
// }) =>
//   `FREQ=WEEKLY;INTERVAL=1;UNTIL=${2024}${
//     month.length == 1 ? '0' + month : month
//   }${day.length == 1 ? '0' + day : day}T160000Z`;

// const dates = getDates();

// console.log(
//   RRULE({
//     day: '2',
//     month: '5',
//     year: 2024,
//   })
// );

// const x = [null, null, 's'];
// x.forEach((s) => console.log(s));

const content = '\t \n \t\n\t\n helloWorld';
const stringX = content.toString();
console.log();

// console.log(stringX.replace('\n', '').replace('\t', ''));
